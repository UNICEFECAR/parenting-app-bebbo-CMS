display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by'
      fields:
        state:
          label: State
        label:
          label: Label
        translator:
          label: Provider
        operations:
          label: Operations
      filters:
        state:
          expose:
            label: State
        translator:
          expose:
            label: Provider
