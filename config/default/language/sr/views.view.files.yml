display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by'
      fields:
        filename:
          label: Name
        status:
          label: Status
      filters:
        status:
          expose:
            label: Status
