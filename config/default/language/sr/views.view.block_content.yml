display:
  default:
    display_title: Default
    display_options:
      fields:
        operations:
          label: Operations
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by'
