display:
  default:
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      fields:
        type:
          label: Typ
        operations:
          label: Operácie
      filters:
        type:
          expose:
            label: Typ
