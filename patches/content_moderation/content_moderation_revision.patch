diff --git docroot/core/modules/content_moderation/src/EntityOperations.php docroot/core/modules/content_moderation/src/EntityOperations.php
index 52d2ca39..478d32df 100644
--- docroot/core/modules/content_moderation/src/EntityOperations.php
+++ docroot/core/modules/content_moderation/src/EntityOperations.php
@@ -116,7 +116,8 @@ public function entityPresave(EntityInterface $entity) {
       // This entity is default if it is new, the default revision, or the
       // default revision is not published.
       $update_default_revision = $entity->isNew()
-        || $current_state->isDefaultRevisionState()
+        || (!$entity->isNew() && isset($entity->original))
+        ||  $current_state->isDefaultRevisionState()
         || !$this->moderationInfo->isDefaultRevisionPublished($entity);
 
       // Fire per-entity-type logic for handling the save process.
